# memory-fundamentals
A database returns what was written. A memory engine reconstructs what can be reached, now, from a cue, under decay, association, and tier physics, without losing a live trace because the first index was the wrong one.
